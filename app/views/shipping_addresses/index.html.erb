<h1>配属先登録/一覧</h1>
<%= form_for(@shipping_adresses) do |f| %>
<h1>郵便番号</h1>
<%= f.text_field :postcpde %>
<h1>住所</h1>
<%= f.text_field :adress %>
<h1>宛名</h1>
<%= f.text_field :name %>
<%= f.submit '登録する' %>
<% end %>
<table>
	<thead>
		<tr>
			<th>郵便番号</th>
			<th>住所</th>
			<th>宛名</th>
			<th colspan="2"></th>
		</tr>
	</thead>
	<tbody>
	 <% @shipping_addresses.each do |shipping_address| %>
      <tr>
      <td><%= shipping_address.postcpde %></td>
      <td><%= shipping_address.adress %></td>
      <td><%= shipping_address.name %></td>
      <td><%= link_to '編集', edit_shipping_adress_path(shipping_address.id) %></td>
      <td><%= link_to '削除', shipping_addresses_path(shipping_address.id), method: :delete %></td>
	  </tr>
	</tbody>
</table>

